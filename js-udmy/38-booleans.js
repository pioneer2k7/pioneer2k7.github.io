"use strict"

//booleans + operator
// === = identical & same type
let greeting = "Hi"
let a = (greeting + "!") === "Hi!";
console.log(a); // true

// !== = not identical

let b = (greeting + "!") !== "Hi!" 
console.log(b); // false weil es NICHT ungleich